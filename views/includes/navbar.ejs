<nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm sticky-top border-bottom">
  <div class="container">
    <!-- Brand -->
    <a class="navbar-brand d-flex align-items-center gap-2 fw-bold" href="/listings">
      <i class="fa-solid fa-compass fa-lg"></i> WonderLust
    </a>

    <!-- Toggler for mobile -->
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="navbar-nav  ms-lg-3 ">
      <form class="d-flex" role="search" method="GET" action="/listings">
        <input class="form-control me-2" type="search" name="q" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-success" type="submit">Search</button>
      </form>      
    </div>
    <!-- Links -->
    <div class="collapse navbar-collapse justify-content-end" id="navbarNavAltMarkup">
      <div class="navbar-nav gap-3 align-items-center">
        <a class="nav-link text-dark fw-semibold" href="/listings">Explore</a>
        <a class="nav-link text-dark fw-semibold" href="/listings/new">Add New Listing</a>

        <!-- Auth Buttons -->
        <div class="d-flex gap-2 ms-lg-4 mt-2 mt-lg-0">
          <% if (!currUser) { %>
            <a href="/login" class="btn btn-outline-primary px-3 rounded-pill">Login</a>
            <a href="/signup" class="btn btn-primary px-3 rounded-pill">Sign Up</a>
          <% } %>
          <% if (currUser) { %>
            <a href="/logout" class="btn btn-danger px-3 rounded-pill">Logout</a>
          <% } %>
        </div>
      </div>
    </div>
  </div>
</nav>
